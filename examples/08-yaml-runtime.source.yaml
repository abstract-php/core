section:
  '@':
    class: hero
  '#':
    - h1: YAML Runtime Example
    - ':if':
        '@':
          test:
            ':logic:var': showDetails
        '#':
          - p: Details are visible from context.
        ':else':
          - p: Details are hidden.
